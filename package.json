{
  "name": "icdrrmo",
  "private": true,
  "description": "ICDRRRMO SMART Emergency Response — workspace scripts",
  "scripts": {
    "infra:up": "docker compose up -d postgres redis",
    "infra:down": "docker compose down",
    "build": "npm --prefix backend run build && npm --prefix admin run build",
    "build:backend": "npm --prefix backend run build",
    "build:admin": "npm --prefix admin run build",
    "start:prod": "npm --prefix backend run start:prod",
    "start:admin": "npm --prefix admin run start",
    "dev:api": "npm --prefix backend run start:dev",
    "dev:admin": "npm --prefix admin run dev",
    "prisma:generate": "npm --prefix backend exec -- prisma generate",
    "migrate": "npm run db:migrate",
    "prisma": "npm --prefix backend exec -- prisma",
    "db:migrate": "npm --prefix backend exec -- prisma migrate deploy",
    "db:seed": "npm --prefix backend exec -- prisma db seed",
    "seed": "npm run db:seed",
    "db:setup": "docker compose up -d postgres redis && npm --prefix backend exec -- prisma migrate deploy && npm --prefix backend exec -- prisma db seed",
    "docker:full": "docker compose up --build -d",
    "docker:seed": "docker compose exec api npx prisma db seed",
    "firebase:seed": "npm --prefix infra/firebase install && npm --prefix infra/firebase run seed",
    "firebase:deploy-firestore": "npx -y firebase-tools deploy --only firestore --project icdrrmo-b204e",
    "deploy:hosting": "npm --prefix admin run export && npx -y firebase-tools@15.17.0 deploy --only hosting --project icdrrmo-b204e --non-interactive"
  }
}
