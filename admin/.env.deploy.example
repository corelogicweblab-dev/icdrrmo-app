# Copy to admin/.env.deploy (gitignored) for local Firebase export, or set as GitHub secrets for CI.
NEXT_PUBLIC_API_URL=https://icdrrmo-api.onrender.com/api/v1
NEXT_PUBLIC_WS_URL=https://icdrrmo-api.onrender.com
# Optional — map tiles on SMART dashboards
# NEXT_PUBLIC_MAPBOX_TOKEN=
