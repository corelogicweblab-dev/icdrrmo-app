# Production static export — copy to `.env.deploy` (gitignored) before `npm run deploy:hosting`
NEXT_PUBLIC_API_URL=https://icdrrmo-backend.onrender.com/api/v1
NEXT_PUBLIC_WS_URL=https://icdrrmo-backend.onrender.com

# Windy — whitelist icdrrmo-b204e.web.app + icdrrmo-b204e.firebaseapp.com on api.windy.com/keys
# NEXT_PUBLIC_WINDY_API_KEY=
