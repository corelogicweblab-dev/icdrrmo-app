"use client";

import type { ReactElement } from "react";

/** Route-change logo overlay removed — instant navigation without a loading flash. */
export function RouteLogoTransition(): ReactElement | null {
  return null;
}
